1. seperate the code of quote service to:
    - controller: keep in the original directory
    - model: create Quote model in lib/quote
2. add test cases for the quote service:
    - create test cases for the Quote model
    - use Mockery to generate mock implementations of the quote model
    - add test cases for each API in protocols/quote
3. add GitHub Actions workflow for running tests:
4. add Redis to cache security IDs 
5. create a command line tool to create partitions for the ohlcva tables:
    - remove or update examples/create_partitions.py accordingly
    - update the Makefile to include the new command line tool for creating partitions
6. stream the data from DB when querying OHLCVAs instead of loading all data into memory at once:
    - update the GetOHLCVAs API to return a stream of data instead of a list
    - if streaming from DB is not feasible, implement pagination to return data in chunks
    - update the client code to handle the streamed data appropriately
7. add HTTP APIs based on the existing gRPC APIs
8. add logging (prefer rs/zerolog)
9. add metrics and monitoring to the quote service:
    - use a monitoring library like Prometheus to collect metrics on the quote service's performance and resource usage
    - set up alerts to notify the team of any issues or anomalies in the quote service's behavior
10. create a login and authentication service (with Keycloak)
    - support line and google login
    - add authentication middleware to the quote service
